/*
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 * Click nbfs://nbhost/SystemFileSystem/Templates/Classes/Class.java to edit this template
 */
package edu.centralenantes.projettp;

/**
 *
 * @author selli
 */
public class TestExceptions {
     public static void main(String[] args) {
        /*
        //NullPointerException : Java essaye d'accéder à un objet dont la référence est en fait à null
        String s = null;
        System.out.println(s.length());
        
        //ArrayIndexOutOfBoundsException : on a tenté d'accéder à un tableau dans un indice illégal
        int[] tab = new int[3];
        System.out.println(tab[5]);
         
        //ArithmeticException : une opération arithmétique illégale a été calculée
        int x = 10 / 0;

        //ClassCastException : une opération de transtypage illégale a été effectuée
        Object o = "texte";
        Integer n = (Integer) o;

        //NumberFormatException : la conversion d'une chaîne de caractères vers un type numérique a échoué car le format n'est pas le bon
        String nb = "12abc";
        int val = Integer.parseInt(nb);

        //StackOverflowError : la pile d'appel Java a explosé
        recursive();

        //ConcurrentModificationException : lorsque l'on essaye de supprimer un élément d'une collection Java alors qu'on la parcourt
        java.util.List<String> list = new java.util.ArrayList<>();
        list.add("A");
        list.add("B");
        list.add("C");
        for (String item : list) {
            list.remove(item);
        }
    */
    }
}
